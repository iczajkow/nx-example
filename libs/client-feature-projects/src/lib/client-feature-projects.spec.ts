import { clientFeatureProjects } from './client-feature-projects';

describe('clientFeatureProjects', () => {
  it('should work', () => {
    expect(clientFeatureProjects()).toEqual('client-feature-projects');
  });
});
